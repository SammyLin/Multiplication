export type Mission = {
  id: string
  multiplicand: number
  multiplier: number
  prompt: string
  narrativeHook: string
  answer: number
  choices: number[]
  focusTable: number
  rewardHint: string
}
